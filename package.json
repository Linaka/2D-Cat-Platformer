{
  "name": "homeward-cat-platformer",
  "version": "1.0.0",
  "private": true,
  "description": "A story-rich 2D cat platformer with skill progression and a forest path home.",
  "type": "module",
  "scripts": {
    "build": "node --check src/game.js",
    "dev": "python3 -m http.server 8765 --bind 127.0.0.1",
    "start": "python3 -m http.server 8765 --bind 127.0.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
